Example

```js
const TablePagination = require('./').default;

<TablePagination
  labelRowsPerPage="Items per page:"
  rowsPerPageOptions={[5, 10, 25]}
  page={1}
  count={200}
  rowsPerPage={10}
/>;
```
