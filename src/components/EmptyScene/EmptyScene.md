Example

```js
const EmptyScene = require('./EmptyScene').default;
const Button = require('../Button').default;

<div>
  <EmptyScene text="You have not created any branches yet.">
    <Button>Create a branch</Button>
  </EmptyScene>
</div>;
```
